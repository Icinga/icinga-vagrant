[icingaweb2]
backend             = "db"
resource            = "icingaweb2_db"

; consider using autologin for apache controlled auth
; requires SetEnv REMOTE_USER "icingaadmin" in icingaweb2.conf apache conf file
; [autologin]
; backend = external
